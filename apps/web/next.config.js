/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Removed experimental typedRoutes which can cause 404 issues in production
};

module.exports = nextConfig;


